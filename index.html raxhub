<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Rax Hub</title>
  <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@600&display=swap" rel="stylesheet">
  <style>
    body {
      margin: 0;
      font-family: 'Orbitron', sans-serif;
      background: linear-gradient(145deg, #0d0d0d 60%, #1a0000);
      color: #fff;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
      padding: 30px;
      position: relative;
      overflow: hidden;
    }

    h1 {
      font-size: 3em;
      color: #ff2e2e;
      text-shadow: 0 0 15px #ff0000, 0 0 30px #aa0000;
      margin-bottom: 20px;
      animation: bounce 2s infinite;
    }

    @keyframes bounce {
      0%, 100% {
        transform: translateY(0);
      }
      50% {
        transform: translateY(-10px);
      }
    }

    p {
      font-size: 1.2em;
      max-width: 600px;
      color: #ffcccc;
    }

    a.button {
      display: inline-block;
      margin-top: 30px;
      padding: 15px 30px;
      font-size: 1.2em;
      background: #ff2e2e;
      color: #000;
      border: none;
      border-radius: 10px;
      text-decoration: none;
      font-weight: bold;
      box-shadow: 0 0 15px #ff0000, 0 0 40px #aa0000;
      transition: transform 0.2s ease, box-shadow 0.3s ease;
    }

    a.button:hover {
      transform: scale(1.05);
      box-shadow: 0 0 25px #ff0000, 0 0 60px #ff1a1a;
    }

    .lightning {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-image: url('https://i.imgur.com/B4JNjPb.gif');
      background-size: cover;
      opacity: 0.1;
      pointer-events: none;
      mix-blend-mode: screen;
      animation: flicker 3s infinite;
    }

    @keyframes flicker {
      0%, 100% { opacity: 0.1; }
      50% { opacity: 0.3; }
    }

    .card {
      z-index: 1;
      background: rgba(0, 0, 0, 0.7);
      padding: 40px;
      border-radius: 20px;
      backdrop-filter: blur(5px);
      box-shadow: 0 0 30px rgba(255, 0, 0, 0.2);
    }

    .logo {
      width: 120px;
      height: 120px;
      border-radius: 50%;
      margin-bottom: 20px;
      animation: pulse 2s infinite ease-in-out;
    }

    @keyframes pulse {
      0%, 100% {
        transform: scale(1);
        box-shadow: 0 0 10px #ff0000;
      }
      50% {
        transform: scale(1.1);
        box-shadow: 0 0 20px #ff0000;
      }
    }
  </style>
</head>
<body>

  <div class="lightning"></div>

  <div class="card">
    <img class="logo" src="https://i.imgur.com/XHZ6t0W.png" alt="Rax Hub Logo">
    <h1>Rax Hub</h1>
    <p>Welcome to Rax Hub! Join our community for exciting giveaways, exclusive events, and much more. We’re charging up with electric energy and thunderous fun!</p>
    <a class="button" href="https://discord.gg/cfUh5wVh" target="_blank" onclick="playSound()">Join Our Discord</a>
  </div>

  <audio id="thunder-sound" src="https://www.soundjay.com/nature/sounds/thunder-1.mp3" preload="auto"></audio>

  <script>
    function playSound() {
      const sound = document.getElementById('thunder-sound');
      sound.volume = 0.5;
      sound.play();
    }
  </script>

</body>
</html>
